export type ChartOptions = {
    width: number,
    height: number,

    dateRange: DateRange,
    segmentDateInterval: DateInterval,
    stickDateInterval: DateInterval,

    valueRange: ValueRange
    valueInterval: ValueInterval
}
