export default {
    //debug
    debugtime: false,
    debugprice: false,
    debuglatest: true,

    //events
    zoomspeed: 100,

    //pixi
    forceCanvas: true,
    autoStart: false,
    autoDensity: true,
    antialias: false,
    resolution: Math.ceil(window.devicePixelRatio),

    //chart
    padding: {
        left: -0.1, // %
        right: 0.3819, // % golden
        top: 0.1, // %
        bottom: 0.2, // %
    }
}
