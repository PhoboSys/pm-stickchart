export declare class ScrollEvent extends Event {
    readonly mouseX: number;
    readonly mouseY: number;
    readonly dragX: number;
    readonly dragY: number;
    constructor(mouseX: number, mouseY: number, dragX: number, dragY: number);
}
