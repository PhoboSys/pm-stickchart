export declare class CurrencyFormatter {
    #private;
    static DEFAULT: string;
    static formatSymboled(amount: any, currency: any, options: any): string;
    static formatUnsymboled(amount: any, currency: any, options: any): string;
    static formatNamed(amount: any, currency: any, options: any): string;
    static formatDefault(amount: any, options: any): string;
}
